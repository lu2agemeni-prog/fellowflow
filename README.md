# FellowFlow - Family Medicine Fellowship Management System

نظام رقمي متكامل لإدارة برامج الزمالة الطبية، يبدأ ببرنامج طب الأسرة ويصمم للتوسع ليشمل جميع التخصصات الطبية.

## 🚀 المميزات

- ✅ **إدارة المستخدمين**: نظام مصادقة كامل مع أدوار متعددة (متدرب، مدرب، مدير)
- ✅ **تتبع الحضور**: تسجيل الحضور بالموقع GPS
- ✅ **إدارة التدويرات**: جدولة ومتابعة التخصصات التدريبية
- ✅ **تقييم المهارات**: نظام DOPs (Direct Observation of Procedural Skills)
- ✅ **الاختبارات**: بنك أسئلة واختبارات إلكترونية
- ✅ **المحاضرات**: إدارة المحاضرات والدورات التدريبية
- ✅ **المكتبة**: مستودع للأدلة العلمية والموارد
- ✅ **التقارير**: تقارير وتحليلات شاملة

## 🛠️ التقنيات المستخدمة

- **Frontend**: React 18 + TypeScript + Vite
- **Styling**: Tailwind CSS + shadcn/ui
- **Backend**: Supabase (PostgreSQL + Auth + Storage)
- **Routing**: React Router v6
- **Icons**: Lucide React

## 📦 التثبيت والتشغيل

### 1. استنساخ المشروع

```bash
git clone https://github.com/yourusername/fellowflow.git
cd fellowflow
```

### 2. تثبيت الاعتماديات

```bash
npm install
```

### 3. إعداد متغيرات البيئة

```bash
cp .env.example .env
```

قم بتحديث ملف `.env` ببيانات Supabase الخاصة بك:

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

### 4. تشغيل المشروع محلياً

```bash
npm run dev
```

المشروع سيعمل على: `http://localhost:5173`

### 5. البناء للإنتاج

```bash
npm run build
```

## 🚀 النشر على Vercel

### الطريقة الأولى: عبر CLI

```bash
# تثبيت Vercel CLI
npm i -g vercel

# تسجيل الدخول
vercel login

# النشر
vercel
```

### الطريقة الثانية: عبر GitHub

1. ارفع المشروع على GitHub
2. سجل دخولك على [Vercel](https://vercel.com)
3. اضغط "Add New Project"
4. اختر المشروع من GitHub
5. أضف متغيرات البيئة (Environment Variables):
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
6. اضغط "Deploy"

## 📁 هيكل المشروع

```
fellowflow/
├── src/
│   ├── components/
│   │   ├── layout/          # مكونات التخطيط (Sidebar, Header, etc.)
│   │   └── ui/              # مكونات shadcn/ui
│   ├── contexts/
│   │   └── AuthContext.tsx  # سياق المصادقة
│   ├── lib/
│   │   ├── supabase.ts      # دوال Supabase
│   │   └── utils.ts         # دوال مساعدة
│   ├── pages/
│   │   ├── auth/            # صفحات المصادقة
│   │   ├── trainee/         # صفحات المتدرب
│   │   ├── trainer/         # صفحات المدرب
│   │   └── admin/           # صفحات المدير
│   ├── types/
│   │   └── index.ts         # أنواع TypeScript
│   ├── App.tsx              # المكون الرئيسي
│   └── main.tsx             # نقطة الدخول
├── public/                  # الملفات الثابتة
├── .env.example             # نموذج متغيرات البيئة
├── package.json
├── tailwind.config.js
├── tsconfig.json
└── vite.config.ts
```

## 👥 الأدوار والصلاحيات

| الدور | الصلاحيات |
|-------|-----------|
| **متدرب** | عرض بياناته، تسجيل الحضور، الاختبارات، المحاضرات |
| **مدرب** | تقييم المتدربين، إدارة المحاضرات، اعتماد الحضور |
| **مدير** | إدارة المستخدمين، البرامج، المراكز، التقارير |

## 🔧 إعداد Supabase

### 1. إنشاء مشروع جديد

1. سجل دخولك على [Supabase](https://supabase.com)
2. اضغط "New Project"
3. اختر اسم المشروع وكلمة المرور

### 2. تشغيل SQL Migration

افتح SQL Editor في Supabase Dashboard ونفذ:

```sql
-- الجداول الأساسية
CREATE TABLE profiles (
  id UUID REFERENCES auth.users ON DELETE CASCADE PRIMARY KEY,
  full_name TEXT NOT NULL,
  email TEXT UNIQUE NOT NULL,
  role TEXT DEFAULT 'trainee',
  fellowship_number TEXT,
  program_id UUID,
  current_year INTEGER DEFAULT 1,
  status TEXT DEFAULT 'active',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- المزيد من الجداول...
```

(انظر ملف `database/01_enhanced_schema.sql` للمخطط الكامل)

### 3. إعداد المصادقة

1. اذهب إلى Authentication > Settings
2. فعّل "Email Confirmations" (اختياري)
3. أضف Site URL: `http://localhost:5173` للتطوير

### 4. إعداد RLS (Row Level Security)

```sql
-- مثال على RLS
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own profile" ON profiles
  FOR SELECT USING (auth.uid() = id);
```

## 📝 سجل التغييرات

### v1.0.0 (2024-11-07)
- ✅ إطلاق النسخة الأولى
- ✅ نظام المصادقة الكامل
- ✅ لوحة تحكم المتدرب
- ✅ لوحة تحكم المدرب
- ✅ لوحة تحكم المدير
- ✅ تتبع الحضور
- ✅ إدارة المهارات
- ✅ الاختبارات والمحاضرات

## 🤝 المساهمة

نرحب بمساهماتكم! للمساهمة:

1. Fork المشروع
2. أنشئ فرعاً جديداً (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push للفرع (`git push origin feature/amazing-feature`)
5. افتح Pull Request

## 📄 الترخيص

هذا المشروع مرخص بموجب [MIT License](LICENSE).

## 📧 التواصل

للاستفسارات والدعم:
- البريد الإلكتروني: support@fellowflow.com
- مركز المساعدة: https://help.fellowflow.com

---

**FellowFlow** - تحويل التعليم الطبي نحو المستقبل الرقمي 🚀
